# LLM config for classify_repository_aimodel (demo / tests).
# Copy to .env and set your keys. .env is gitignored.
REPO_CLASSIFIER_API_KEY=sk-your-key
REPO_CLASSIFIER_MODEL_NAME=gpt-4o
# Optional (defaults used if unset):
# REPO_CLASSIFIER_TEMPERATURE=0.1
# REPO_CLASSIFIER_TIMEOUT=60
